[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dockerify"
version = "0.0.0"
description = "Dockerify: Docker governance hub for World of Shadows"

[project.scripts]
dockerify = "dockerify.tools.hub_cli:main"
